import 'package:flutter/material.dart';

class InfiniteSize extends StatelessWidget {
  const InfiniteSize({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: double.infinity);
  }
}
